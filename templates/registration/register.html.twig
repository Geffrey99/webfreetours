{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
{% include 'nav.html.twig' %}
    
{# <section class="login">
  <div class="container py-4">
    <div class="row g-0 align-items-center">
      <div class="col-lg-6 mb-5 mb-lg-0">
        <div class="card cascading-right" style="
            background: hsla(0, 0%, 100%, 0.55);
            backdrop-filter: blur(30px);
            ">
          <div class="card-body p-5 shadow-5 text-center">
            <h2 class="fw-bold mb-5">Registrate</h2>
          
            {{ form_errors(registrationForm) }}
            {{ form_start(registrationForm) }}
              <!-- 2 column grid layout with text inputs for the first and last names -->
              <div class="row">
                <div class="col-md-6 mb-4">
                  <div class="form-outline">
                    {{ form_row(registrationForm.nombre) }}
                  </div>
                </div>
                <div class="col-md-6 mb-4">
                  <div class="form-outline">
                     {{ form_row(registrationForm.Password) }}
                
                  </div>
                </div>
              </div>

              <!-- email input -->
              <div class="form-outline mb-4 required">
                {{ form_row(registrationForm.email) }}
              </div>

              <!-- foto input -->
              <div class="form-outline mb-4 required">
             {{ form_row(registrationForm.foto) }}
              </div>

              <!-- Checkbox -->
              <div class="form-check d-flex justify-content-center mb-4">
                 {{ form_row(registrationForm.agreeTerms) }}
                </label>
              </div>

              <!-- Submit button -->
              <button type="submit" class="btn btn-primary btn-block mb-4">
                Sign up
              </button>
             {% for flash_error in app.flashes('verify_email_error') %}
              <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
            {% endfor %}
            {{ form_end(registrationForm) }}
          </div>
        </div>
      </div>

        <div class="col-lg-6 mb-5 mb-lg-0">
        <img src="{{ asset('img/jaenc.jpg') }}" class="w-100 rounded-4 shadow-4"
          alt="" />
        </div>
    </div>
  </div>
  <!-- Jumbotron -->
</section>
<!-- Section: Design Block -->  #}


{# 
<section class="vh-100" style="background-color: #eee;">
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-12 col-xl-11">
        <div class="card text-black" style="border-radius: 25px;">
          <div class="card-body p-md-5">
            <div class="row justify-content-center">
              <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Sign up</p>
 {{ form_errors(registrationForm) }}
            {{ form_start(registrationForm) }}
                <form class="mx-1 mx-md-4">

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                       {{ form_row(registrationForm.nombre) }}
                      <label class="form-label" for="form3Example1c">Your Name</label>
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                        {{ form_row(registrationForm.email) }}
                      <label class="form-label" for="form3Example3c">Your Email</label>
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                        {{ form_row(registrationForm.Password) }}
                      <label class="form-label" for="form3Example4c">Password</label>
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-key fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      {{ form_row(registrationForm.foto) }}
                      <label class="form-label" for="form3Example4cd">Foto de perfil</label>
                    </div>
                  </div>

                  <div class="form-check d-flex justify-content-center mb-5">
                     {{ form_row(registrationForm.agreeTerms) }}
                    <label class="form-check-label" for="form2Example3">
                      I agree all statements in <a href="#!">Terms of service</a>
                    </label>
                  </div>

                  <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                    <button type="submit" class="btn btn-primary btn-lg">Register</button>
                  </div>

                </form>
  {{ form_end(registrationForm) }}
              </div>
              <div class="col-md-10 col-lg-6 col-xl-7 d-flex align-items-center order-1 order-lg-2">

                <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-registration/draw1.webp"
                  class="img-fluid" alt="Sample image">

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
 #}
<section class="login">
  <div class="container h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-lg-12 col-xl-11">
        <div class="card text-black" style="border-radius: 25px; margin-top: 20px;">
          <div class="card-body p-md-5">
            <div class="row justify-content-center">
              <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">

                <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Registrate</p>
     {{ form_errors(registrationForm) }}
            {{ form_start(registrationForm) }}
               

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                       <label class="form-label" for="form3Example1c">Nombre</label>
                       {{ form_row(registrationForm.nombre) }}
                     
                      {{ form_errors(registrationForm.nombre) }} <!-- muestra el error del campo nombre -->
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                         <label class="form-label" for="form3Example3c">Correo Electrónico</label>
                        {{ form_row(registrationForm.email) }}
               
                      {{ form_errors(registrationForm.email, {'attr': {'class': 'text-danger'}}) }} <!-- muestra el error del campo email in red color -->
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                        <label class="form-label" for="form3Example4c">Contraseña</label>
                        {{ form_row(registrationForm.Password) }}
                      {{ form_errors(registrationForm.Password) }} <!-- muestra el error del campo password -->
                    </div>
                  </div>

                  <div class="d-flex flex-row align-items-center mb-4">
                    <i class="fas fa-key fa-lg me-3 fa-fw"></i>
                    <div class="form-outline flex-fill mb-0">
                      <label class="form-label" for="form3Example4cd">Foto</label>
                      {{ form_row(registrationForm.foto) }}
                     
                      {{ form_errors(registrationForm.foto) }} <!-- muestra el error del campo foto -->
                    </div>
                  </div>

                  <div class="form-check d-flex justify-content-center mb-5">
                     {{ form_row(registrationForm.agreeTerms) }}
                    <label class="form-check-label" for="form2Example3">
                        Acepto todas las declaraciones en <a href="#!">Términos de servicio</a>
                    </label>
                    {{ form_errors(registrationForm.agreeTerms) }} <!-- muestra el error del campo agreeTerms -->
                  </div>

                  <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                    <button type="submit" class="btn btn-primary btn-lg">Registrarte</button>
                  </div>
  {{ form_end(registrationForm) }}
              </div>
              <div class="col-md-10 col-lg-6 col-xl-7 d-flex align-items-center order-1 order-lg-2">

                {# <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-registration/draw1.webp"
                  class="img-fluid" alt="Sample image"> #}

                   <img src="{{ asset('img/m.jpg') }}" class="w-100 rounded-4 shadow-4">

              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


{% endblock %}
